// Write a program to calculate sum of first N naturals numbers.

#include <stdio.h>
int main()
{
    int n, i, sum;

    printf("Enter the value of n:");
    scanf("%d", &n);

    for (i = 1; i <= n; i++)
    {
        sum == 0 ;
        sum = sum + i;
        
    }
    printf("%d", sum);

    return 0;
}